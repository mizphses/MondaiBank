%h1 Listing quizzes

%table
  %thead
    %tr
      %th Name
      %th Subj
      %th Tags
      %th Content
      %th Admin
      %th
      %th
      %th

  %tbody
    - Quiz.where(:admin => current_user.email).each do |quiz|
      %tr
        %td= quiz.name
        %td= quiz.subj
        %td= quiz.tags
        %td= quiz.content
        %td= quiz.admin
        %td= link_to '情報を表示', quiz
        %td= link_to '問題表紙情報を編集', edit_quiz_path(quiz)
        %td= link_to '問題を編集', edit_quiz_path(quiz)
        %td= link_to '削除', quiz, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Quiz', new_quiz_path
